<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.lixiyun.server.mapper.PersonRoleMapper">
    <insert id="saveUserRole">
        insert into person_role(person_id, role_id) values (#{personId},
            (select id from role where name = #{roleName} and status = 0))
    </insert>
</mapper>