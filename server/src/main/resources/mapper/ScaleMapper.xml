<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.lixiyun.server.mapper.ScaleMapper">
    <select id="selectMyPage" resultType="org.lixiyun.pojo.entity.scale.Scale">
        SELECT
        id,
        scale_name,
        description,
        question_count,
        scale_category_id,
        status,
        score_rule,
        created_time,
        created_by,
        updated_time,
        updated_by,
        deleted
        FROM scale
        <where>
            <if test="scaleName != null and scaleName != ''">
                AND scale_name LIKE CONCAT('%', #{scaleName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="isDeleted != null">
                AND deleted = #{isDeleted}
            </if>
        </where>
        ORDER BY created_time DESC
    </select>
</mapper>