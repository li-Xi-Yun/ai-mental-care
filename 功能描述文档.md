# AI Mental Care 功能描述文档

## 1. 功能概述

本文档描述 AI Mental Care 系统已实现的会话管理、对话管理和情绪分析相关功能。这些功能参考主流 AI 问答产品（如 ChatGPT、Claude）的交互设计，提供完整的心理健康咨询会话管理能力。

## 2. 功能模块详解

### 2.1 会话管理模块

#### 2.1.1 会话列表分页展示

**接口地址**：`GET /conversation/list`

**功能说明**：
- 展示当前用户的所有会话列表
- 支持分页查询
- 按更新时间逆序排列（最新更新的会话排在最前面）

**请求参数**：
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| pageNum | Integer | 否 | 1 | 页码 |
| pageSize | Integer | 否 | 10 | 每页数量 |

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "records": [
      {
        "id": 123456789,
        "name": "关于工作压力的咨询",
        "currentRound": 5,
        "createdTime": "2026-03-19 10:00:00",
        "updatedTime": "2026-03-19 11:30:00"
      }
    ]
  }
}
```

---

#### 2.1.2 会话名称修改

**接口地址**：`PUT /conversation/{id}/name`

**功能说明**：
- 修改指定会话的名称
- 仅允许修改自己创建的会话

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会话ID |

**请求体**：
```json
{
  "name": "新的会话名称"
}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": null
}
```

---

#### 2.1.3 会话删除

**接口地址**：`DELETE /conversation/{id}`

**功能说明**：
- 删除指定会话
- 同时删除该会话关联的对话记录、情绪分析数据和诊断书
- 采用逻辑删除，数据不会真正从数据库中移除

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会话ID |

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": null
}
```

---

### 2.2 对话管理模块

#### 2.2.1 对话记录分页展示

**接口地址**：`GET /conversation/{conversationId}/memory`

**功能说明**：
- 展示指定会话下的所有对话记录
- 支持分页查询
- 按创建时间正序排列（最早的对话排在最前面，符合对话流程）

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| conversationId | Long | 是 | 会话ID |

**请求参数**：
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| pageNum | Integer | 否 | 1 | 页码 |
| pageSize | Integer | 否 | 20 | 每页数量 |

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 50,
    "records": [
      {
        "id": 1,
        "content": "你好，我最近感觉很焦虑",
        "type": "USER",
        "roundNum": 1,
        "createdTime": "2026-03-19 10:00:00"
      },
      {
        "id": 2,
        "content": "你好，我理解你的感受。能告诉我更多关于你焦虑的情况吗？",
        "type": "ASSISTANT",
        "roundNum": 1,
        "createdTime": "2026-03-19 10:00:05"
      }
    ]
  }
}
```

---

#### 2.2.2 对话记录删除

**接口地址**：`DELETE /conversation/memory/{id}`

**功能说明**：
- 删除指定的对话记录
- 同时删除该轮次关联的情绪分析数据

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 对话记录ID |

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": null
}
```

---

### 2.3 情绪分析模块

#### 2.3.1 诊断书展示

**接口地址**：`GET /conversation/{conversationId}/diagnosis`

**功能说明**：
- 获取指定会话的情绪诊断报告
- 如果该会话尚未生成诊断书，返回 `hasDiagnosis: false`
- 前端可根据 `hasDiagnosis` 字段展示默认画面

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| conversationId | Long | 是 | 会话ID |

**响应示例（有诊断书）**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "hasDiagnosis": true,
    "coreEmotionLabel": "焦虑",
    "coreEmotionIntensity": "中度",
    "emotionRiskLevel": "medium",
    "diagnosisContent": "根据对话分析，用户存在中度焦虑情绪...",
    "emotionAdjustSuggestion": "建议适当休息，减少工作压力...",
    "emotionTrend": "波动",
    "positiveEmotionRatio": 0.2,
    "negativeEmotionRatio": 0.6,
    "neutralEmotionRatio": 0.2,
    "updatedTime": "2026-03-19 11:30:00"
  }
}
```

**响应示例（无诊断书）**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": null,
    "hasDiagnosis": false
  }
}
```

---

#### 2.3.2 情绪分析分页展示

**接口地址**：`GET /conversation/{conversationId}/emotion-analysis`

**功能说明**：
- 展示指定会话下的所有情绪分析记录
- 支持分页查询
- 按创建时间逆序排列（最新的分析排在最前面）

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| conversationId | Long | 是 | 会话ID |

**请求参数**：
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| pageNum | Integer | 否 | 1 | 页码 |
| pageSize | Integer | 否 | 10 | 每页数量 |

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 10,
    "records": [
      {
        "id": 1,
        "roundNum": 3,
        "emotionLabel": "焦虑",
        "emotionSubLabel": "轻度焦虑",
        "emotionScore": 0.85,
        "emotionTrend": "上升",
        "positiveEmotionRatio": 0.15,
        "negativeEmotionRatio": 0.75,
        "createdTime": "2026-03-19 10:30:00"
      }
    ]
  }
}
```

---

#### 2.3.3 情绪分析详情（含对话）

**接口地址**：`GET /conversation/emotion-analysis/{analysisId}/dialogue`

**功能说明**：
- 获取情绪分析详情
- 同时返回该轮次对应的对话记录（用户消息和助手回复）

**路径参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| analysisId | Long | 是 | 情绪分析ID |

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "roundNum": 3,
    "emotionLabel": "焦虑",
    "emotionSubLabel": "轻度焦虑",
    "emotionScore": 0.85,
    "emotionTrend": "上升",
    "positiveEmotionRatio": 0.15,
    "negativeEmotionRatio": 0.75,
    "analysisContent": "详细分析内容...",
    "createdTime": "2026-03-19 10:30:00",
    "dialogues": [
      {
        "id": 5,
        "content": "我最近工作压力很大，经常失眠",
        "type": "USER",
        "roundNum": 3,
        "createdTime": "2026-03-19 10:25:00"
      },
      {
        "id": 6,
        "content": "我理解你的困扰。工作压力导致的失眠是很常见的问题...",
        "type": "ASSISTANT",
        "roundNum": 3,
        "createdTime": "2026-03-19 10:25:10"
      }
    ]
  }
}
```

---

## 3. 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 403 | 无权访问该资源 |
| 3000 | 会话不存在 |
| 3002 | 会话未找到 |

---

## 4. 数据模型关系

```
User (用户)
  └── Conversation (会话) 1:N
        ├── ConversationMemory (对话记录) 1:N
        ├── EmotionAnalysis (情绪分析) 1:N
        └── EmotionDiagnosis (诊断书) 1:1
```

---

## 5. 权限控制

所有接口均需要用户登录认证，用户只能查看和管理自己创建的会话数据。系统会在每次操作时验证会话所有权。

---

## 6. 文件清单

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `pojo/src/main/java/org/lixiyun/pojo/vo/conversation/ConversationVO.java` | 会话列表展示 VO |
| `pojo/src/main/java/org/lixiyun/pojo/vo/conversation/ConversationMemoryVO.java` | 对话记录展示 VO |
| `pojo/src/main/java/org/lixiyun/pojo/vo/conversation/EmotionAnalysisVO.java` | 情绪分析展示 VO |
| `pojo/src/main/java/org/lixiyun/pojo/vo/conversation/EmotionDiagnosisVO.java` | 诊断书展示 VO |
| `pojo/src/main/java/org/lixiyun/pojo/vo/conversation/EmotionAnalysisDetailVO.java` | 情绪分析详情 VO |
| `pojo/src/main/java/org/lixiyun/pojo/dto/conversation/UpdateConversationNameDTO.java` | 会话名称修改 DTO |

### 修改文件

| 文件路径 | 说明 |
|----------|------|
| `server/src/main/java/org/lixiyun/server/service/ConversationService.java` | 会话服务接口 |
| `server/src/main/java/org/lixiyun/server/service/impl/ConversationServiceImpl.java` | 会话服务实现 |
| `server/src/main/java/org/lixiyun/server/controller/conversation/ConversationController.java` | 会话控制器 |
