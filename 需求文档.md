# AI Mental Care 功能需求文档

## 1. 概述

本文档描述 AI Mental Care 系统的会话管理、对话管理和情绪分析相关功能需求。这些功能参考主流 AI 问答产品（如 ChatGPT、Claude）的交互设计。

## 2. 功能模块

### 2.1 会话管理模块

#### 2.1.1 会话列表分页展示

**功能描述**：用户可以查看自己创建的所有会话列表，支持分页展示，按更新时间逆序排列（最新更新的会话排在最前面）。

**接口设计**：
- 请求方式：`GET /conversation/list`
- 请求参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | pageNum | Integer | 否 | 页码，默认1 |
  | pageSize | Integer | 否 | 每页数量，默认10 |
- 响应数据：
  - 会话ID
  - 会话名称
  - 创建时间
  - 更新时间
  - 当前轮次

#### 2.1.2 会话名称修改

**功能描述**：用户可以修改会话的名称，便于后续查找和管理。

**接口设计**：
- 请求方式：`PUT /conversation/{id}/name`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | id | Long | 是 | 会话ID |
- 请求体：
  ```json
  {
    "name": "新的会话名称"
  }
  ```
- 响应：操作结果

#### 2.1.3 会话删除

**功能描述**：用户可以删除不需要的会话，删除后该会话及其关联的对话记录、情绪分析数据都将被逻辑删除。

**接口设计**：
- 请求方式：`DELETE /conversation/{id}`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | id | Long | 是 | 会话ID |
- 响应：操作结果

---

### 2.2 对话管理模块

#### 2.2.1 对话上下文信息分页展示

**功能描述**：用户可以查看指定会话下的所有对话记录，支持分页展示，按创建时间正序排列（最早的对话排在最前面，符合对话流程）。

**接口设计**：
- 请求方式：`GET /conversation/{conversationId}/memory`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | conversationId | Long | 是 | 会话ID |
- 请求参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | pageNum | Integer | 否 | 页码，默认1 |
  | pageSize | Integer | 否 | 每页数量，默认20 |
- 响应数据：
  - 记录ID
  - 消息内容
  - 消息类型（USER/ASSISTANT/SYSTEM）
  - 轮次
  - 创建时间

#### 2.2.2 对话删除

**功能描述**：用户可以删除指定的对话记录，删除后该对话及其关联的情绪分析数据将被逻辑删除。

**接口设计**：
- 请求方式：`DELETE /conversation/memory/{id}`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | id | Long | 是 | 对话记录ID |
- 响应：操作结果

---

### 2.3 情绪分析模块

#### 2.3.1 诊断书展示

**功能描述**：用户可以查看指定会话的情绪诊断报告。如果该会话尚未生成诊断书，则返回默认提示信息。

**接口设计**：
- 请求方式：`GET /conversation/{conversationId}/diagnosis`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | conversationId | Long | 是 | 会话ID |
- 响应数据：
  - 诊断书ID（可能为空）
  - 核心情绪标签
  - 核心情绪强度
  - 情绪风险等级
  - 诊断内容
  - 情绪调节建议
  - 情绪趋势
  - 更新时间
  - 是否存在诊断书标识

#### 2.3.2 情绪分析分页展示

**功能描述**：用户可以查看指定会话下的所有情绪分析记录，支持分页展示，按创建时间逆序排列（最新的分析排在最前面）。

**接口设计**：
- 请求方式：`GET /conversation/{conversationId}/emotion-analysis`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | conversationId | Long | 是 | 会话ID |
- 请求参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | pageNum | Integer | 否 | 页码，默认1 |
  | pageSize | Integer | 否 | 每页数量，默认10 |
- 响应数据：
  - 分析ID
  - 轮次
  - 情绪标签
  - 情绪细分标签
  - 情绪置信度
  - 情绪趋势
  - 正向情绪占比
  - 负向情绪占比
  - 创建时间

#### 2.3.3 情绪分析关联对话展示

**功能描述**：用户点击某条情绪分析记录时，可以查看该轮次对应的对话信息（仅展示该轮次的一问一答）。

**接口设计**：
- 请求方式：`GET /emotion-analysis/{analysisId}/dialogue`
- 路径参数：
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | analysisId | Long | 是 | 情绪分析ID |
- 响应数据：
  - 情绪分析信息
  - 对话记录列表（该轮次的用户消息和助手回复）

---

## 3. 数据模型关系

```
User (用户)
  └── Conversation (会话) 1:N
        ├── ConversationMemory (对话记录) 1:N
        ├── EmotionAnalysis (情绪分析) 1:N
        └── EmotionDiagnosis (诊断书) 1:1
```

## 4. 权限控制

所有接口均需要用户登录认证，用户只能查看和管理自己创建的会话数据。

## 5. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 会话不存在 | 返回404错误 |
| 会话不属于当前用户 | 返回403错误 |
| 诊断书不存在 | 返回空数据，前端展示默认画面 |
| 对话记录不存在 | 返回404错误 |

## 6. 前端交互参考

参考 ChatGPT 的交互设计：
- 会话列表展示在左侧边栏
- 点击会话进入对话详情页
- 情绪分析以卡片形式展示，支持点击查看详情
- 诊断书以独立页面或弹窗形式展示
